# Base URL of the blog-panel API the dashboard talks to.
VITE_API_BASE_URL=http://localhost:8080
# Same, but used when the frontend is built/served inside Docker.
DOCKER_API_BASE_URL=http://localhost:4000
